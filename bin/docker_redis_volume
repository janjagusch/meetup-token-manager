#!/usr/bin/env sh
#
# This script creates a Docker volume called meetup-tokens if it does not exist yet.

if docker volume list -q | grep -q meetup-tokens
then
	echo 'volume already exists.'
else
	echo 'creating volume.'
	docker volume create meetup-tokens
fi;