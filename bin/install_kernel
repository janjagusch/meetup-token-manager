#!/bin/bash
#
# This script installs the virtual environment as a kernel into Jupyter.

poetry run python \
	-m ipykernel install \
	--user \
	--name meetup-token-manager \
	--display-name "Python (meetup-token-manager)"
